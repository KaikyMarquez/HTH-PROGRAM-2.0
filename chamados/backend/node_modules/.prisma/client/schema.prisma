generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id       Int    @id @default(autoincrement())
  name     String
  email    String @unique
  password String
  role     Role

  createdTickets Ticket[] @relation("CreatedTickets")
  handledTickets Ticket[] @relation("HandledTickets")
}

model Ticket {
  id           Int      @id @default(autoincrement())
  eventName    String
  standName    String
  standAddress String?
  description  String
  status       Status   @default(ABERTO)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  userId Int
  user   User @relation("CreatedTickets", fields: [userId], references: [id])

  technicianId Int?
  technician   User? @relation("HandledTickets", fields: [technicianId], references: [id])
}

enum Role {
  ADMIN
  TECNICO
  VISUALIZADOR
}

enum Status {
  ABERTO
  ANDAMENTO
  FECHADO
}
